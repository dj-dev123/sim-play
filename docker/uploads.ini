; Laravel validates activity/profile images with max:2048 (2MB). PHP's own
; defaults (2M/8M) sit right at that edge and reject real phone-camera
; photos before Laravel's validation even runs, surfacing as a vague
; "failed to upload" error instead of the actual size-limit message.
upload_max_filesize = 10M
post_max_size = 12M
